# Copiar a .env en este directorio y rellenar.
POSTGRES_PASSWORD=cambiame
ADMIN_TOKEN=cambiame-token-largo-y-aleatorio
# Orígenes permitidos para la API admin (el panel ya la consume vía proxy /api,
# así que normalmente no hace falta tocar esto).
CORS_ORIGINS=*
