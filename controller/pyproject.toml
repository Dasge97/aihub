[project]
name = "aihub-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["aihub-kit", "docker>=7.0", "huggingface_hub>=0.23"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
